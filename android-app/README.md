# Garden Helper — Android Prototype

This is a minimal Expo React Native prototype demonstrating:
- Map view with OpenStreetMap tiles
- Add persistent markers for garden items
- Fetch weather for marker coordinates (Open-Meteo)

To run:

```bash
cd android-app
npm install
npm run start
```
